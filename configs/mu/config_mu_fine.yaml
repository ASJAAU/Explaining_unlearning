base: "configs/config.yaml"

training:
  epochs: 3
  lr: 0.005

model:
  exp: "MU03"

data:
  target_format: "counts"
  augment: True
  classes:
  - "bicycle"
  - "motorcycle"
  - "vehicle"

unlearning:
  method: "fine_tune"

wandb:
  enabled: True
  tags:
    - MU
    - MSE
    - Human
    - single_count
    - fine_tune
    - leafy-darkness-85