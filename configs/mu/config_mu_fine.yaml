base: "configs/config.yaml"

training:
  epochs: 5
  lr: 0.005

model:
  exp: "MU02"

data:
  target_format: "counts"
  augment: True
  classes:
  - "bicycle"
  - "motorcycle"
  - "vehicle"

unlearning:
  method: "fine_tune"

wandb:
  enabled: True
  tags:
    - MU
    - MSE
    - Human
    - single_count
    - fine_tune
    - resilient-tree-3