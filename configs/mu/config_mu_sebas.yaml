base: "configs/config.yaml"

training:
  epochs: 3
  lr: 0.005

model:
  exp: "MU03"

data:
  target_format: "counts"
  augment: True
  classes:
  - "bicycle"
  - "motorcycle"
  - "vehicle"

unlearning:
  method: "sebastian_unlearn"
  amount: 0.99
  rand_init: False

wandb:
  enabled: True
  tags:
    - MU
    - MSE
    - Human
    - single_count
    - sebastian
    - "99"
    - leafy-darkness-85